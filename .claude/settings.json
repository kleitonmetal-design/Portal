{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | grep -q 'instrucoes-projeto-plataforma-gestao-pci.md' && echo '{\"systemMessage\": \"✅ Thyreos specification file updated. Syncing to Supabase documentos table (slug: plataforma-thyreos-pci)...\"}' || true",
            "statusMessage": "Syncing Thyreos spec to Supabase..."
          }
        ]
      }
    ]
  }
}
